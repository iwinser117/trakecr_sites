<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - Tracker Sites</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', 'Oxygen', 'Ubuntu', sans-serif;
            background: linear-gradient(135deg, #dc3545 0%, #c82333 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .error-container {
            background: white;
            padding: 60px 40px;
            border-radius: 12px;
            box-shadow: 0 10px 40px rgba(0,0,0,0.2);
            text-align: center;
            max-width: 500px;
        }

        .error-code {
            font-size: 5em;
            font-weight: bold;
            color: #dc3545;
            line-height: 1;
            margin-bottom: 10px;
        }

        .error-title {
            font-size: 1.8em;
            color: #333;
            margin-bottom: 20px;
        }

        .error-message {
            color: #666;
            font-size: 1.1em;
            margin-bottom: 30px;
            line-height: 1.6;
        }

        .error-icon {
            font-size: 4em;
            margin-bottom: 20px;
        }

        .btn-home {
            display: inline-block;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 14px 40px;
            border-radius: 6px;
            text-decoration: none;
            font-weight: 600;
            transition: transform 0.2s, box-shadow 0.2s;
            margin: 10px 5px;
        }

        .btn-home:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 20px rgba(102, 126, 234, 0.4);
        }

        .btn-back {
            display: inline-block;
            background: #f0f0f0;
            color: #333;
            padding: 14px 40px;
            border-radius: 6px;
            text-decoration: none;
            font-weight: 600;
            transition: background 0.2s;
        }

        .btn-back:hover {
            background: #e0e0e0;
        }

        .error-details {
            margin-top: 40px;
            padding-top: 30px;
            border-top: 1px solid #eee;
            text-align: left;
        }

        .error-detail-item {
            margin-bottom: 15px;
            font-size: 0.9em;
            color: #666;
        }

        .error-detail-item strong {
            color: #333;
        }

        .error-detail-value {
            background: #f8f9fa;
            padding: 10px;
            border-radius: 4px;
            margin-top: 5px;
            font-family: 'Courier New', monospace;
            color: #dc3545;
            word-break: break-all;
        }

        @media (max-width: 600px) {
            .error-container {
                padding: 40px 25px;
            }

            .error-code {
                font-size: 3.5em;
            }

            .error-title {
                font-size: 1.5em;
            }

            .error-icon {
                font-size: 3em;
            }
        }
    </style>
</head>
<body>
    <div class="error-container">
        <div class="error-icon">⚠️</div>
        <div class="error-code"><%= status || 500 %></div>
        <h1 class="error-title"><%= message || 'Error del servidor' %></h1>
        <p class="error-message">
            Se ha producido un error al procesar tu solicitud. Intenta de nuevo más tarde.
        </p>
        
        <div>
            <a class="btn-home" href="/">🏠 Ir al inicio</a>
            <a class="btn-back" href="javascript:history.back()">← Atrás</a>
        </div>

        <% if (process.env.NODE_ENV === 'development') { %>
            <div class="error-details">
                <div class="error-detail-item">
                    <strong>Detalles del error (modo desarrollo):</strong>
                    <div class="error-detail-value"><%= error?.stack || error?.message || 'Sin detalles' %></div>
                </div>
            </div>
        <% } %>
    </div>
</body>
</html>
